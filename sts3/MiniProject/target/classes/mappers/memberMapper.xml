<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miniproject.mappers.memberMapper">
	<select id="curDate" resultType="string">
		select now()
	</select>
	
	<select id="selectUserId" resultType="com.miniproject.domain.Member">
		select * from member where userId = #{userId}
	</select>

	<update id="updateUserPoint">
		<selectKey keyProperty="howMuch" resultType="int" order="BEFORE">
			select howMuch from pointPolicy where why = #{why};
		</selectKey>
		update member set userPoint = userPoint + #{howMuch} where userId = #{userId};
	</update>
	
<!-- 	<select id="login" resultType="Member"> -->
<!--       select m.*, u.newFileName as memberImg from member m -->
<!--       inner join uploadedfile u on m.userImg = u.no -->
<!--       where m.userId = #{userId} and m.userPw = sha1(md5(#{userPw})) -->
<!--    </select> -->
   <select id="login" resultType="Member">
   	select m.*, u.newFileName as memberImg from member m inner join uploadedfile u on m.userImg = u.no where userId = #{userId} and userPw = sha1(md5(#{userPw}))
   </select>
   
   <update id="updateSessionInfo">
      update member set sessionLimit = #{sessionLimit}, sessionKey = #{sessionKey} where userId = #{userId}
   </update>
   

</mapper>
